# Python dependencies for simulation, testing, and host-side tools.
# Install with:  pip install -r requirements.txt
# Or let the workflow manage everything:  make setup

# Simulation / verification
cocotb
cocotb-test
pytest
gitpython

# Numerical processing (weight generation, event processing)
numpy

# Host-side camera emulator and event tools (preview window support)
opencv-python

# UART communication with the FPGA
pyserial

#plotting
matplotlib